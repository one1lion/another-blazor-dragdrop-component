@page "/dnd-comps"
@using DragAndDrop.Components
@using DragAndDrop.Data

<DragAndDropContainerComponent Items="items" GroupNameProperty="@nameof(Foo.GroupName)">
</DragAndDropContainerComponent>

@code {
  List<Foo> items;

  protected override void OnInitialized() {
    items = SampleDataService.GenerateSampleFooItems();
  }
}
